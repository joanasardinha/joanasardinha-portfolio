{
  "buildCommand": "npm install && cd portfolio && npm run build",
  "outputDirectory": "portfolio/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
